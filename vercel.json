{
  "buildCommand": "pnpm --filter @workspace/the-system run build",
  "outputDirectory": "artifacts/the-system/dist/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
