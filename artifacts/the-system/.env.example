# When deploying the frontend to Vercel, set this to your API server's URL.
# Example: https://your-api.railway.app
# Leave empty to use relative paths (works on Replit where both services share a proxy).
VITE_API_URL=
